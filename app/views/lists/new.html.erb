<%= render "navbar" %>

<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto">
      <div class="flat-form">
        <h1>Add a new List</h1>
        <div class="mt-3">
          <%= render "form", list: @list %>
          <br>
          <%= link_to "Cancel and go back to Lists", lists_path, class: "btn btn-secondary" %>
        </div>
      </div>
    </div>
  </div>
</div>
